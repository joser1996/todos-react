import React from 'react';

//using ES6 arrow funcitons same as function Header() {}
const Header = () => {
    return(
        <header>
            <h1>todos</h1>
        </header>
    );
}

export default Header;